#!/bin/sh

export CONF=/etc/skeeto
mkdir -p $CONF
cd $CONF

git clone --bare https://github.com/skeeto/dotfiles.git dotfiles.git
curl -L https://skeeto.s3.amazonaws.com/wallpapers/Kelsey-Wellons-Wallpaper-Collection-1.tar >wallpaper.tar
